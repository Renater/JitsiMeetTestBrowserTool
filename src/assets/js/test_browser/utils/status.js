/**
 * Enum for test running statuses
 *
 * @type {{PAUSED: number, STOPPED: number, PROCESSING: number, WAITING: number, ENDED: number}}
 */
window.TestStatuses = {
    WAITING: 0,
    PROCESSING: 1,
    PAUSED: 2,
    STOPPED: 3,
    ENDED: 4,
};